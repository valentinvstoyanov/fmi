 1.
 a)Уравнението да се реши числено, като се използва явния метод на Ойлер при n = 4, 50, 250 и решението да се начертае в една координатна система с графиката на точното решение;

sol[n_] := Module[{T, t0, h, w, y0, y,},
  T = 1;
  t0 = 0;
  h = (T - t0) / n;
  w = Table[t0 + i*h, {i, 0, n}];
  y0 = 0.01;
  y = Table[y0, n + 1];
  For [i = 1, i <= n , i++,
   y[[i + 1]] = y[[i]] + h*10*y[[i]]*(1 - y[[i]])
   ];
  Transpose[{w, y}]
  ]

exactSol[x_] := 0.01/(0.01 + 0.99*(E^(-10*x)))
Show[ListPlot[sol[50], PlotStyle -> Red], Plot[exactSol[x], {x, 0, 1}]]
